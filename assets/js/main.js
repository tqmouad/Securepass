/**
 * main.js
 * Global scripts for the landing page.
 * Future features (SP-002, SP-003...) will be initialized here.
 */

document.addEventListener("DOMContentLoaded", () => {
    console.log("SecurePass initialized.");
});